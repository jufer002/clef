<%= form_with(model: section_content, local: true) do |form| %>
  <% if section_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section_content.errors.count, "error") %> prohibited this section_content from being saved:</h2>

      <ul>
        <% section_content.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.text_field :section_id %>
  </div>

  <div class="field">
    <%= form.label :lesson_id %>
    <%= form.text_field :lesson_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
