<div class="card">
    <div class="card-body">
        <div class="card-title"> 
            <%= link_to course.title, course, class:"card-title-link" %>
        </div>
        <%= link_to course.user.username, course.user, class:"card-subtitle-link mb-2 text-muted" %> 
        <p class="card-text"><%= course.description %></p>
    </div>
</div>