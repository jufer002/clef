<%= link_to course, class:"card-link" do %>
    <div class="card">
        <div class="card-body">
            <h4 class="card-title"> <%= course.title %> </h4>
            
            <h6 class="card-subtitle mb-2 text-muted"> <%= course.user.username %> </h6>
            <p class="card-text"><%= course.description %></p>
        </div>
    </div>
<% end %>