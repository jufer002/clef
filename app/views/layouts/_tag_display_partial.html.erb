   
<h4>
    <% if content.tags.empty? %>
    No tags
    <% else %>
    <div class="wrapped"> 
        Tags: <% content.tags.each do |content_tag| %>
            <%= link_to content_tag, class:"badge" do %>
                <%= content_tag.name %>
            <% end %>
        <% end %>
    </div>
    <% end  %>
</h4>
