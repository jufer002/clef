<div class="container">
  <h1>Search</h1>
    <%= form_tag(search_path, method: 'get') do %>
      <div class="container">
        <label>Lesson Title</label>
        <%= text_field_tag(:lesson_title, "") %>
      </div>

      <%= submit_tag("Search", class: 'btn') %>
    <% end %>
</div>
